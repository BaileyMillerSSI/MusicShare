# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: music-matcher
services:  
  app:
    language: dotnet
    project: ./MusicShare.AppHost/MusicShare.AppHost.csproj
    host: containerapp
